# AGENTS.md

Windows-only C#/.NET 10 WPF launcher for FiveM. Design decisions and UX philosophy live in `DOC.md` (in English); commit messages are English conventional commits. UI strings and documentation are English. Development is strictly TDD (RED -> minimal implementation -> GREEN -> REFACTOR), one layer at a time. The refactor step is mandatory: after every GREEN, improve the code — enforcing DRY, KISS, SOLID and YAGNI — with the suite still green, before starting the next test.

## Commands
- Build: `dotnet build FiveMServerLauncher.slnx`
- Test all: `dotnet test`
- Run one test class: `dotnet test --filter FullyQualifiedName~CfxServiceTests` (xUnit)
- Publish portable single-file win-x64 (`CFXLauncher.exe`, self-contained, one file; native
  WPF libs self-extract to temp at startup). First build, then publish with `--no-build` (a
  publish that recompiles WPF markup in the `_wpftmp` temp project cannot resolve the
  Grpc.Tools-generated `Master` types — CS0246). `-r win-x64` alone does NOT imply
  self-contained (.NET 8+ breaking change), and a framework-dependent build output reused by
  `--no-build` would publish without the runtime — so `SelfContained=true` is mandatory at
  BUILD time:
  `dotnet build src/FiveMServerLauncher/FiveMServerLauncher.csproj -c Release -r win-x64 -p:SelfContained=true`
  `dotnet publish src/FiveMServerLauncher/FiveMServerLauncher.csproj -c Release -r win-x64 --self-contained --no-build -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -p:DebugType=none -p:DebugSymbols=false -o dist`
  Single-file props stay on the command line, never in the csproj. Compression (`EnableCompressionInSingleFile`)
  drops the deliverable from ~140 MB to ~62 MB with zero runtime risk; IL trimming is NOT used
  (runs at build time, which the `--no-build`/`_wpftmp` constraint forbids, and WPF is reflection-heavy
  so trimming risks runtime breakage — that's why `PublishTrimmed=false` stays in the csproj).
- Solution uses the new `.slnx` format: `dotnet sln FiveMServerLauncher.slnx add <project>`.
- Requires .NET 10 SDK. Verify with `dotnet --version` before running anything.

## Current state (verified; do not "fix" blindly)
- Product name is **CFX Launcher** (`Core/AppInfo`: `ProductName`/`Author`/`Title`, the single
  user-visible naming source bound by `MainWindow.xaml` title bar + footer). Executable is
  `CFXLauncher.exe` (`<AssemblyName>CFXLauncher</AssemblyName>` + `<RootNamespace>
  FiveMServerLauncher</RootNamespace>` — namespaces/types unchanged; do NOT switch to
  `TargetName`, it breaks .NET 10 WPF markup compilation (MC3050 in the `_wpftmp` temp project)).
- Data is portable: `Configuration/PortableDataDirectory` resolves `launcher-settings.json` /
  `saved-servers.json` beside the running exe (injectable `Func<string?>`, default
  `Environment.ProcessPath` — `AppContext.BaseDirectory` is the single-file extraction dir, not
  the exe's home), and `Configuration/LegacyDataMigration` best-effort copies the old
  `%localappdata%\FiveMServerLauncher` files on first run when the portable dir is empty
  (portable wins on conflict; legacy kept as backup, never deleted). Both are wired in
  `App.xaml.cs`; new seams are intentional (composition-root owned).
- Build and all tests pass (`dotnet test`).
- `Domain/ServerResolver.ResolveAsync` accepts the four DOC.md address forms (`CfxId`, `cfx.re/join/<id>` with/without scheme, `IP:port`, `domain:port`) via `ServerAddress.Classify` and returns a `ServerProfile` (`CfxId`, `Address`, `ProjectName`, `GameClient`, `Requirements`, `IsCfxValidated`). There is an overload `ResolveAsync(string, SavedServer?)` that merges the saved server's manual Steam/Discord flags into `Requirements` via `ServerRequirements.ForConnection`, so `ServerProfile.Requirements` is always the *effective* connection requirements. `ServerRequirements` (sealed record) carries CFX-published `GameBuild`, `PureMode`, `RequestSteamTicket`, `DefaultBuild`, `ReplaceExecutable`, `PoolSizesIncrease`, `SteamRequired` (from published `sv_enforceSteamAuth`), `DiscordRequired` (all nullable when not published). `ForConnection` makes the manual flag *additive*: a saved server's `RequiresSteam = true` always requires Steam (even when the server publishes `sv_enforceSteamAuth = false`), otherwise the published value wins when present, else the manual fallback.
  - `CfxId`/`CfxJoinUrl` → `CfxService` (+`ServerRequirementsResolver`), `IsCfxValidated=true`; unknown form → `InvalidAddressException`.
  - `IpPort`/`DomainPort` → `ServerCatalog` lookup (streamRedir catalog). Found → validated profile built from catalog vars; not found/unavailable → connectable unvalidated profile (`IsCfxValidated=false`, `Address` = raw address, no invented fields). `domain:port` resolves DNS→IP for the match via `IDnsResolver` (seam); DNS failure or catalog outage also degrade to unvalidated (no exception).
- `Domain/FiveMLaunchOptions` (single seam, sealed class, immutable, private ctor — only the validated `Create`/`FromServerProfile` factories construct it; as a class it has no `with`, so the invariant is compiler-enforced) serializes launch intent: `Address` (any address classified as `CfxJoinUrl`/`IpPort`/`DomainPort`, or null when opening directly), `GameClient`, `GameBuild`, `PureMode`, `SecondClient`. `FromServerProfile` uses `CfxId` when set (join form) and falls back to the profile's raw `Address` for unvalidated servers. `ToUri()` returns `<scheme>://connect/<addr>?-b<build>?-pure_<nivel>` (`redm://` for `GameClient.RedM`, `fivem://` otherwise; null when no address or `FiveMEnhanced`); `ToCommandLineArgs()` returns an `ImmutableArray<string>` `-b<build> -pure_<nivel> [-cl2]` (empty for `FiveMEnhanced`, truly read-only). `Domain/ServerAddress` is the single owner of the `cfx.re/join/` form and of address classification (`ServerAddressKind`, `Classify`) + validation (IP octets/port 1-65535, domain labels/TLD-with-letter, no whitespace).
- `Service/CfxService.cs` uses the *requested* `cfxId` as `CfxServerInfo.CfxId` (the `/single/` response `EndPoint` is a connection endpoint, not the id — do NOT use it as a cfx id). `requestSteamTicket` distinguishes `on` → `true`, `off` → `false`, absent/invalid → `null`. Variable mapping is shared via `Service/CfxVars` (`TryGetGameClient`/`TryGetInt`/`TryGetBool`/`TryGetString`/`MapSteamTicket`; gamename→`GameClient`, ints, strict `"true"`/`"false"` booleans, raw strings, steam ticket).
- `Service/ServerCatalog` (seam, `HttpClient` + injectable `TimeProvider`/TTL) downloads `https://frontend.cfx-services.net/api/servers/streamRedir/` (binary frames: uint32 LE length + protobuf `master.Server`), caches with ~5 min TTL, and answers `LookupByIpPortAsync` (matches `connectEndPoints`) / `LookupByEndPointAsync` (matches `EndPoint`). In catalog frames `EndPoint` IS the canonical cfx server id (so `ServerResolver.BuildValidatedProfile` may use it as `CfxId`) — unlike the `/single/` response, where `EndPoint` is a connection endpoint. Outage/corrupt frames → no match, never throws (only `HttpRequestException`/`TaskCanceledException` are swallowed as outages; other exceptions propagate). Protobuf codegen via `Google.Protobuf` + `Grpc.Tools` (`Proto/master.proto`, generated `Master.Server`/`ServerData`, no `Player`).
- UI is minimal and unbound: `Views/MainView.xaml` wires the connect flow (TextBox `ServerAddress`, "ENTER SERVER" button `Command="{Binding ConnectCommand}"`, status `TextBlock` bound to `StatusText`) plus the saved-servers panel (ListBox bound to `SavedServers` with rows rendered as `[icon] name [gameTag] [status]` (icon visible only when the row has enrichment; the per-game colored tag `GameClientToBrushConverter` (FiveM orange/Enhanced blue/RedM red) shows only once the game is known; the status slot shows `players/max` when online, `OFFLINE` for a CfxId row not in the current catalog, `UNRESOLVED` when no CfxId — plain name otherwise; `BytesToImageSourceConverter` turns `Icon` bytes into a `BitmapImage`), the edit pencil appearing only on the selected row, `DeleteServerCommand` / `OpenAddServerDialogCommand` buttons, and selection bound to `SelectedServer` filling `ServerAddress`; add/edit uses an in-window overlay dialog (`IsServerDialogOpen`) with `DialogServerName`/`DialogServerAddress`/`DialogRequiresSteam`/`DialogRequiresDiscord`, `DialogError` inline validation, `ServerDialogTitle`, `EditingServer`, and SAVE/CANCEL (`SaveServerDialogCommand`/`CancelServerDialogCommand`) implementing address-keyed add/edit/remove semantics); business logic stays out of XAML code-behind. The OPEN split button is data-bound: `FiveMButton` shows `SelectedOpenClientLabel` and commands `OpenClientCommand`; the dropdown is an `ItemsControl` bound to `AvailableOpenClients` (installed clients only) with each item commanding `SelectOpenClientCommand` with the option as `CommandParameter`; the only code-behind is the show/hide toggle (`DropdownButton_Click`/`ClientOption_Click`). the settings panel and Dev Mode are mutually exclusive (opening one closes the other), lists use a thin dark view-scoped `ScrollBar` style, and the window/executable icon is `Assets/launcher.ico` (`ApplicationIcon`, multi-size derived from `launcher.png` at repo root). The settings panel (gear "⚙ SETTINGS" button → `SettingsCommand` → `IsSettingsOpen` via `BooleanToVisibilityConverter`) carries the preferred-client ComboBox (`PreferredClientOption`) and the auto-launch CheckBox (`AutoLaunch`); both persist through `ConfigurationRepository` on change. A DEV MODE button (`ToggleDevModeCommand` → `IsDevMode`) swaps the normal OPEN area for dev controls: Game Build TextBox (`DevGameBuild`) and Pure Mode combo (`DevPureMode`, `SelectedValue` on `sys:Int32` tags), both persisted on change through the repository, a CLIENT toggle (session-only `DevClient`/`DevClientLabel`, Legacy ↔ RedM), plus LAUNCH / LAUNCH WITH SECOND CLIENT buttons driving `DevLaunchCommand` with a bool `CommandParameter` (second-client flag is session-only); the swap uses `BooleanToVisibilityConverter` + `InverseBoolToVisibilityConverter`, no code-behind. The `DataContext` is assigned by the composition root (`App.xaml.cs`), which then shows the window and kicks off `InitializeAsync` via `Dispatcher.InvokeAsync` (never sync-blocked: auto-launch's connect involves real network awaits and a `GetAwaiter().GetResult()` at startup deadlocks the WPF dispatcher). `InitializeAsync` seeds the OPEN selection from `LauncherSettings.PreferredClient` (falling back to first installed) and, when `AutoLaunch` + `LastServerAddress` are set, fills the address and runs the normal connect in-line.
- Branch `main` is ahead of `origin/main`; commits are conventional English per the workflow below.

## Architecture
- `Core/Enums`: shared types. `GameClient` = FiveM | FiveMEnhanced | RedM. Note `DOC.md` lists only two — the code is source of truth; `Service/CfxService.cs` maps CFX `gamename` (`gta5`, `gta5enhanced`, `rdr3`) to it.
- `Configuration/`: global launcher settings only. `LauncherSettings` is `PreferredClient` + `AutoLaunch` + `LastServerAddress` (the "open automatically next time" memory — the last successfully launched connect address) + `DevGameBuild` / `DevPureMode` (persisted Dev Mode values; the dev client toggle and `-cl2` are session-only, never persisted). JSON persistence via `ISettingsStorage` -> `FileSettingsStorage` (System.Text.Json + `JsonStringEnumConverter`, `launcher-settings.json` next to the running exe via `PortableDataDirectory`). Never add per-server fields, `Platform`, or `ServerPort` here.
- `Domain/SavedServer` (immutable, private ctor + validated `Create`, `[JsonConstructor]` for persistence) carries `Name`, `Address` (trimmed, classified connectable via `ServerAddress.Classify`), manual `RequiresSteam`/`RequiresDiscord` (`bool?`), optional `CfxId` (enrichment key, null when unknown; derived from CFX-form addresses, or captured at save time for IP:port/domain), and `MatchesAddress` (case-insensitive identity key). `Configuration/IServerRepository` -> `FileServerRepository` persist the saved-servers JSON list (missing/corrupt/invalid entries → empty, never crash; missing `CfxId` in old files → null; `Add` rejects duplicates, `Update` is address-keyed, `Remove` no-ops when unknown); tests use `InMemoryServerRepository`.
- `Service/ServerEnrichmentService` (seam, `IServerEnrichmentService`; `HttpClient` + `ServerCatalog` + optional `IDnsResolver`) powers the enriched saved-server rows: `RefreshAsync` maps the streamRedir catalog snapshot to a `CfxId → ServerPresence(Online, Players, MaxPlayers, Game)` dictionary (`null` = outage, never throws; duplicate EndPoints grouped; `gamename` var → `GameClient` via `CfxVars`), `GetIconAsync(cfxId)` tracks `iconVersion` via `/single/{id}` and caches the PNG bytes keyed by `(CfxId, iconVersion)` (re-download only on version change; corrupt payload → null), and `ResolveCfxIdAsync(address)` returns the catalog `EndPoint` (= canonical cfx id) for IP:port/domain (DNS→IP via the seam) or extracts the id from CFX forms; all outages degrade to null/empty, never throw (swallows `HttpRequestException`/`TaskCanceledException`/`JsonException` only).
- **Composition root**: `App.xaml.cs` `OnStartup` builds the object graph manually (`HttpClient` → shared `ServerCatalog` → `CfxService`/`ServerEnrichmentService` → `ServerResolver` → `GameLauncher` (`GameProcessLauncher` + `ProcessStarter` + `UriSchemeRegistration`, `CitizenFxPreparer` + `ClientInstallLocator` + `CitizenFxConfigWriter`; the same `ClientInstallLocator` instance is shared with `GameLauncher` and `MainViewModel`) → `ExternalAppStarter`/`ExternalAppPreparer` (shared `ProcessReadinessChecker`) → `MainViewModel`) and sets `MainWindow.DataContext`; no DI container, no `StartupUri` in `App.xaml`. It also starts the per-minute enrichment loop on the window dispatcher (cancelled when the window closes); a single `ServerCatalog` is shared between `ServerResolver` and `ServerEnrichmentService` so the streamRedir cache is not duplicated.
- `Launch/` (Application layer; named `Launch` to avoid clashing with `System.Windows.Application` in WPF): `GameLauncher.ConnectAsync(ServerProfile)` primes the Legacy FiveM client config (`ICitizenFxPreparer.PrimeAsync`; best-effort, never throws, never fails the connect, and a throwing preparer is also swallowed by GameLauncher so the `LaunchResult` stays exact), builds `FiveMLaunchOptions` and returns a `LaunchResult` — a sealed record hierarchy (`Connect(Uri)` after delegating the `fivem://connect/...` URI to `IGameProcessLauncher` the process seam, `OpenClient(GameClient)` when the client process was started, `NotInstalled(GameClient)` when no executable resolves, or `StartFailed` on launch exception; no null-union — the type IS the discriminator). When the profile has no URI (e.g. `FiveMEnhanced`), `ConnectAsync` invokes the same open primitive: `GameLauncher.OpenAsync(GameClient)` resolves the executable via `IClientInstallLocator`, starts it through `IGameProcessLauncher.StartExecutableAsync(path)` (a second overload takes CLI args — used by Dev Mode with `UseShellExecute = true`, i.e. ShellExecute-with-args after the single-parameter `fivem://<flag>` route proved insufficient), and reports `OpenClient`/`NotInstalled`/`StartFailed`. `GameLauncher.OpenAsync(FiveMLaunchOptions)` is the Dev Mode entry: serializes `ToCommandLineArgs()` (`-b<build> -pure_<n> -cl2`), resolves the Legacy exe via the locator, starts with args. `Launch/CitizenFxPreparer` owns the "what+when to write" decision for `CitizenFX.ini`: only CFX-validated Legacy FiveM or RedM profiles, `DefaultBuild` from `sv_defaultGameBuild` when `> 0` (else `sv_replaceExeToSwitchBuilds` → `sv_enforceGameBuild`) and `PoolSizesIncrease` verbatim when non-blank — else the key is reset to an empty value so default-pool servers clear stale increases. Real launch is `Launch/GameProcessLauncher` (real `IGameProcessLauncher`): throws `Win32Exception` when the `fivem://` scheme is unregistered (`IUriSchemeRegistration` seam, so `StartFailed` stays reachable), then hands the URI to the Windows shell via `explorer.exe` (`IProcessStarter` seam; `CreateProcess`, no direct `ShellExecuteEx`); direct executable opens go the same shell route (`explorer.exe "<path>"`) — both need shell context (Rockstar/GTA launcher refuses a loader-started child). Shell context is required: `ShellExecuteEx` of the `fivem://` URI direct from the launcher process made FiveM's GTAVLauncher legit flow refuse the GTA boot ("This application should be launched directly from the shell or a web browser") — the URI itself is correct and manual Win+R/`Start-Process` launches work. The scheme-check + shell-launch is shared by
           `GameProcessLauncher`/`ExternalAppStarter` via `Launch/UriShellStarter` (internal helper). `Launch/ExternalAppStarter`
           (real `IExternalAppStarter`) opens a required external app through its URI scheme (`steam://`/`discord://`); an
           unmapped `ExternalApp` value is a no-op (totality guard mirroring `ProcessReadinessChecker`). `Launch/ExternalAppPreparer`
           (`IRequirementReadiness` + `IExternalAppStarter`, injectable wait + `maxAttempts`; defaults 500 ms / 120 ≈ 60 s) is the
           "start the missing required app and wait until it is **ready**" open+check loop: already ready → `true` without touching
           it, else start only when the process is not running, then poll readiness (immediate check first, then wait between
           checks); an app that is *already running but not ready* (e.g. Steam at the login screen) is waited on **without
           restarting**; readiness/start/wait exceptions all degrade to `false`, never throws to the caller.
- `ViewModels/` (MVVM layer): `MainViewModel` (ServerAddress/IsBusy/StatusText/ConnectCommand) drives the connect flow; uses `ServerResolver` + `GameLauncher` + `IServerRepository` + `ExternalAppPreparer` + `IClientInstallLocator` + `ConfigurationRepository` + `IServerEnrichmentService`; matches the typed address to a saved server, resolves (effective requirements), and *prepares every required app* through the preparer (which is a no-op when the app is already ready): for each required app it shows "Starting {app}..." then prepares it — starting the process if absent and waiting until it is *fully ready* (logged in for Steam) — giving up with "Could not start {app}" (no launch) when an app can't be prepared; nothing extra when nothing is required or all are already ready; catches `InvalidAddressException` → "Invalid address" (English UI strings; see migration phase). The VM merges the loaded `LauncherSettings` into itself (`_settings`): `AutoLaunch`/`PreferredClientOption` setters save through the repository immediately, and a successful connect persists the trimmed address as `LastServerAddress` (failures never clobber it). It also owns the direct-open flow: `InitializeAsync` seeds `AvailableOpenClients` from the install locator (FiveM Legacy, then FiveM Enhanced, then RedM; installed only), `SelectedOpenClient`/`SelectedOpenClientLabel` drive the OPEN button, `SelectOpenClientCommand` changes the selection, and `OpenClientCommand` opens the selected client through `GameLauncher.OpenAsync`; connect and open share a `Describe(LaunchResult)` status mapping ("Opening {client}...", "{client} is not installed", "Launch failed"). Display names live in `InstalledClientOption` ("FiveM" / "FiveM Enhanced" / "RedM"). The saved-servers rows are `SavedServerItem` (name/address/steam/discord + enrichment state `CfxId`/`HasCfxId`/`Online`/`Players`/`MaxPlayers`/`StatusLabel` (`players/max`, `OFFLINE`, `UNRESOLVED`)/`Game`/`GameTagLabel`/`HasGame`/`Icon` bytes, `ApplyPresence(ServerPresence)`, `SetIcon(byte[])`, `SetCfxId(string)`); `ServerPresence` now carries a nullable `GameClient` (from the catalog `gamename` var) so rows render the per-game tag, and `ApplyPresence` keeps the last known game when a presence carries no game; `MainViewModel` owns save-time background CfxId capture for IP:port/domain (`TryCaptureCfxId` → `_enrichment.ResolveCfxIdAsync` → repository `Update`, fire-and-forget, dialog never blocked; CFX-form ids are stored synchronously at `SavedServer.Create` and preserved on same-address edits) and the per-minute `RunEnrichmentLoopAsync(cancellationToken, cadence?, delay?)` that applies presence + icons to rows with a CfxId and leaves null-CfxId rows plain (`RefreshServerInfoAsync`; outage → no update, never throws; the loop swallows a throwing refresh so one bad cycle never kills it).
- `Service/CfxService.cs`: queries `https://frontend.cfx-services.net/api/servers/single/{cfxId}`; typed vars include `sv_enforceGameBuild`, `sv_pureLevel`, `sv_defaultGameBuild`, `sv_replaceExeToSwitchBuilds`, `sv_poolSizesIncrease`, `sv_enforceSteamAuth`, `gamename`, `requestSteamTicket`.
- `Service/ServerCatalog.cs` (seam): downloads + caches (TTL) the streamRedir catalog (protobuf frames via `Service/ServerCatalogDecoder` + `Proto/master.proto` codegen); answers `LookupByIpPortAsync`/`LookupByEndPointAsync` and `GetSnapshotAsync` (returns `IReadOnlyList<Master.Server>?`, null on outage vs empty list; `GetServersAsync` = `GetSnapshotAsync() ?? []`). `LookupByEndPointAsync` (match by cfx id) is currently used only by tests but stays as part of the catalog seam contract, ready for future id-based lookups. Never throws on outage/corruption — returns no match. In catalog frames `EndPoint` is the canonical cfx id; in `/single/` responses it is a connection endpoint.
- `Service/CfxVars.cs`: shared variable mapping (`gamename`→`GameClient` via `TryGetGameClient`, ints via `TryGetInt`, strict `"true"`/`"false"` booleans via `TryGetBool`, raw strings via `TryGetString`, `requestSteamTicket` via `MapSteamTicket`) used by `CfxService` and `ServerResolver`.
- `Service/CfxStatusService` (seam, `HttpClient`): downloads `https://citizenfx.statuspage.io/api/v2/summary.json` once per call (no internal cache — the VM's per-minute loop is the throttle) and returns `IReadOnlyDictionary<GameClient, CfxStatus>?` (null on outage/parse failure; swallows `HttpRequestException`/`TaskCanceledException`/`JsonException` only). `CfxStatus` (in `Core/Enums`) = `Operational | Degraded | PartialOutage | MajorOutage | Maintenance | Unknown`; component strings map (`operational`/`degraded_performance`/`partial_outage`/`major_outage`/`under_maintenance`, else `Unknown`) and page `status.indicator` maps (`none`/`minor`/`major`/`critical`, else `Unknown`). FiveM/RedM read their dedicated components (name match first, id return-part); the statuspage publishes **no FiveM Enhanced component**, so FiveMEnhanced reads the page-level indicator instead. `CfxStatusItem` (row VM) holds `DisplayName`/`Status`/`StatusLabel` (`OPERATIONAL`/`DEGRADED`/`PARTIAL OUTAGE`/`OUTAGE`/`MAINTENANCE`/`UNKNOWN`); `CfxStatusToBrushConverter` (green/amber/red/grey, data-only). MainViewModel exposes `FiveMStatus`/`FiveMEnhancedStatus`/`RedMStatus` + ordered `CfxStatuses`, `RefreshCfxStatusAsync` applies a non-null map (null keeps last known; first-ever failure → `Unknown`), called once in `InitializeAsync` and each `RunEnrichmentLoopAsync` cycle. The hardcoded `FIVEM / CFX  ONLINE` block is replaced by a three-row ItemsControl bound to `CfxStatuses`.
- `Service/IRequirementReadiness` (seam): `Task<bool> IsRunningAsync(ExternalApp)` (coarse "process exists" gate) and `Task<bool> IsReadyAsync(ExternalApp)` (the gate the preparer/connect flow wait on). Real `Service/ProcessReadinessChecker` checks process names via an injectable `Func<string,bool>` (default real `Process.GetProcessesByName`); mapping `ExternalApp.Steam`→`steam`, `ExternalApp.Discord`→`Discord`. `IsReadyAsync` for Steam is the **composite probe**: `steam` process AND `steamwebhelper` process (both through the process seam) AND a logged-in session — `ActiveUser` from `HKCU\Software\Valve\Steam\ActiveProcess` read via an injectable `Func<int?>` (default real registry, same seam pattern as `ClientInstallLocator`); Discord ready = running; missing/throwing probe or registry → false, never throws. This deliberately does NOT equate "process running" with "Steam fully loaded/logged in".
- `Service/IClientInstallLocator` (seam): `Task<bool> IsInstalledAsync(GameClient)`, `Task<string?> GetExecutablePathAsync(GameClient)`, `Task<string?> GetInstallDirectoryAsync(GameClient)` (dir only when installed). Real impl `Service/ClientInstallLocator`: Legacy FiveM install dir resolves from the registry `HKCU\Software\CitizenFX\FiveM` -> `Last Run Location` first (injectable `Func<string?>` seam, default real registry) with `%localappdata%\FiveM\FiveM.app` fallback, and counts as installed when `FiveM.exe` exists inside the `.app` dir or as its direct sibling (portable/zip layout); Enhanced probes `%localappdata%\FiveM for GTAV Enhanced\FiveM.exe` directly (no `.app` subfolder); RedM mirrors the Legacy pattern with `HKCU\Software\CitizenFX\RedM` -> `Last Run Location` and the `%localappdata%\RedM\RedM.app\RedM.exe` fallback. Both Legacy-like branches share `ResolveAppStyle(localAppData, provider, folderName, exeName)`. Never test the real OS install (seams replace filesystem and registry calls).
- `Service/ICitizenFxConfigWriter` (seam) + `Service/CitizenFxConfigWriter`: read-modify-write of `[Game]` keys in `CitizenFX.ini` — creates the file when absent, preserves every other key/section verbatim, only rewrites when a target value differs.
- `Domain/ServerResolver.cs`: resolves the four address forms; `IpPort`/`DomainPort` degrade to a connectable unvalidated profile when not published in CFX (or on DNS/catalog failure). `IDnsResolver` is the DNS seam.
- `ConfigurationRepository` and `ServerRequirementsResolver` are intentional seams per `docs/adr/0001-intentional-middle-men-seams.md` (do not inline without reopening that ADR).
- `Domain/ServerAddress` owns the `cfx.re/join/<id>` form and address classification/validation (`ServerAddressKind`, `Classify`, `ExtractCfxId`, `HasServerFormWithNonEmptyId`), reused by `ServerResolver` and `FiveMLaunchOptions`.
- Keep business logic out of XAML code-behind and out of real processes/filesystem so tests stay fast.

## Tests
- xUnit, project targets `net10.0-windows` and references the main project. No mocking library — fake HTTP via `tests/.../Service/FakeHttpMessageHandler`, routed HTTP via `tests/.../Service/RoutedHttpMessageHandler` (multiple byte/text routes, request counts, used by the enrichment service tests), fake time via `tests/.../Service/FakeTimeProvider`, fake DNS via `tests/.../Domain/FakeDnsResolver`, protobuf frame fixtures via `tests/.../Service/TestProtobufFrames`, in-memory settings via `tests/.../Configuration/InMemorySettingsStorage`, temp files via `tests/.../Configuration/TempSettingsDirectory` and `tests/.../Service/TempCitizenFxIni` (`IDisposable`, unique path per test + teardown), and fakes for `Launch/` seams (`FakeGameProcessLauncher`,
           `FakeCitizenFxPreparer`, `FakeClientInstallLocator`, `FakeCitizenFxConfigWriter`,
           `FakeExternalAppStarter`, `StatefulRequirementReadiness`) plus `FakeServerEnrichmentService`
           and fake DNS; all live in the test project.
- JSON fixtures use C# raw strings (`"""..."""`, `$$"""..."""`).
- Naming style: `<Method>_Should<Expectation>` with Given/When/Then comments.

## Workflow: Spec-Driven Development + TDD (agent skills)
- Skills live in `.agents/skills` (registered via `skills.paths` in `opencode.json`; `skills-lock.json` pins versions — update with `npx skills update`).
- Per feature: `to-spec` (or `spec-driven-development` from addyosmani for a full PRD) -> `to-tickets` -> `implement` (drives `/tdd`) -> `code-review`, then commit.
- `tdd` (mattpocock): RED->GREEN loop at pre-agreed seams only, one vertical slice per cycle. After each GREEN, run the mandatory REFACTOR step (DRY/KISS/SOLID/YAGNI) with the suite still green before starting the next test.
- .NET test helpers: `run-tests` before guessing a `dotnet test` command; `find-untested-sources`/`test-anti-patterns`/`test-gap-analysis`/`assertion-quality` for audits; `detect-static-dependencies` + `code-testing-agent` to add coverage.
- `domain-modeling`/`codebase-design` define module seam vocabulary; `diagnosing-bugs` for logic/CFX API bugs.

## Agent skills

### Issue tracker

Issues and specs are tracked as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

## Hard design constraints (from DOC.md)
- RSC is never managed by the launcher. Steam/Discord are per-server `ServerProfile` requirements, never universal.
- Server-published (CFX) requirements win over manual config when connecting; Dev Mode build/pure overrides apply only when opening FiveM directly; one carve-out: a saved server's manual `Requires Steam = true` is additive and always requires Steam, even when the server publishes `sv_enforceSteamAuth = false`.
- Never equate "process started" with "service ready/authenticated".