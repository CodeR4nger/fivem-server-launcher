# 04: Close the ui-connect-flow phase

**What to build:** close-out. Green suite + AGENTS.md with the new `ViewModels` layer, updated test count, conventional commit.

**Blocked by:** 03

**Status:** resolved

- [x] AGENTS.md updated (`ViewModels` layer with `MainViewModel` + `AsyncRelayCommand`; 109 tests).
- [x] Spec `resolved`, tickets 01-03 `resolved` with complete checkboxes.
- [x] Full suite green (109) + `dotnet build` with no errors.
- [x] English conventional commit (pending user confirmation).

## Comments
