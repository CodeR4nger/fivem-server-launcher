# 02: Thin dark scrollbar style

**What to build:** a `Scrollbar` style scoped to `MainView` (thin track, `6` thumb width,
`#3A3A3A` fill, transparent track, matching the dark UI). It applies automatically to any ListBox
in the view.

**Blocked by:** none.

**Status:** resolved

